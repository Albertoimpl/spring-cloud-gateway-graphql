pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/milestone' }
        gradlePluginPortal()
    }
}

rootProject.name = 'spring-cloud-gateway-graphql'

include "edge-gateway"
include "graphql-server"

